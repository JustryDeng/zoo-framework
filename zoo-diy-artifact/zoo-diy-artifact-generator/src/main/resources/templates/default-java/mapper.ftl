package ${mapper.pkg};

import org.apache.ibatis.annotations.Mapper;

/**
* ${mapper.comment!"(non-javadoc)"}
*
* @author ${mapper.author}
* @since ${mapper.since}
*/
@Mapper
public interface ${mapper.name} {

}